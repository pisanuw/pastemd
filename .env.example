# PasteMD configuration
# Copy to .env for local Netlify dev, set the same values in Netlify Site settings.

JWT_SECRET=replace-with-a-long-random-secret
TOKEN_ENCRYPTION_KEY=replace-with-a-different-long-random-secret
APP_URL=https://your-site.netlify.app

# Passwordless email link (Resend)
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxx
AUTH_FROM_EMAIL=PasteMD <noreply@yourdomain.com>

# Google OAuth
# Add https://your-site.netlify.app/api/auth/google/callback as an authorized redirect URI
# in Google Cloud Console → APIs & Services → Credentials → your OAuth 2.0 client.
GOOGLE_CLIENT_ID=xxxxxxxxxx-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-xxxxxxxxxxxxxxxxxxxxxxxx

# Admin accounts (comma-separated emails)
ADMIN_EMAILS=admin@yourdomain.com
